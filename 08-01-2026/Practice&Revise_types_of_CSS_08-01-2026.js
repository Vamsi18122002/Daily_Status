console.log("Types of CSS")
console.log("Inline css -> Inline CSS is used to apply style directly inside an HTML element using the style attribute.")

console.log("Internal css -> Internal CSS is written inside a <style> tag within the <head> section of an HTML page and is used to style a single web page.")
